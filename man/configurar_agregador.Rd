% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/configurar_agregador.R
\name{configurar_agregador}
\alias{configurar_agregador}
\title{Configuration function for Poll Aggregator}
\usage{
configurar_agregador(
  pesquisas = NULL,
  resultado_eleicao_passada = NULL,
  resultado_eleicao_atual = NULL,
  historico_pesquisas = NULL,
  data_inicio = "01/01/2025",
  data_fim = Sys.Date(),
  cargo = "Presidente",
  ambito = "Brasil",
  cenario = "",
  candidaturas_1t = c("Lula", "Flávio", "Tarcísio", "Ratinho Jr."),
  candidaturas_2t = c("Lula", "Flávio", "Tarcísio", "Ratinho Jr.", "Bolsonaro"),
  direita_eleicao_atual = c("Bolsonaro", "Flávio", "Tarcísio", "Caiado"),
  direita_eleicao_passada = "Bolsonaro",
  esquerda_eleicao_atual = c("Lula", "Haddad"),
  esquerda_eleicao_passada = "Lula",
  eleicao_passada_primeiro_turno = "2/10/2022",
  eleicao_passada_segundo_turno = "30/10/2022",
  stan_cores = pmin(parallel::detectCores(), 4),
  stan_chains = 4,
  stan_warmup = 500,
  stan_sampling = 500,
  stan_init = 0.1,
  stan_adapt_delta = 0.99,
  saida_bases_tratadas = "resultados_agregador/bases_tratadas",
  saida_modelos_brutos = "resultados_agregador/modelos_brutos"
)
}
\arguments{
\item{pesquisas}{Path to a CSV file or URL containing current poll data. Defaults to a GitHub Raw URL.}

\item{resultado_eleicao_passada}{Path to a CSV file containing results from the previous election. Defaults to a GitHub Raw URL.}

\item{resultado_eleicao_atual}{Path to a CSV file containing results for the current election (useful for retrospective model). Defaults to a GitHub Raw URL.}

\item{historico_pesquisas}{Path to a CSV/RDS file containing historical poll data. If NULL (default), uses the package's internal dataset.}

\item{data_inicio}{Start date for filtering polls (default: "01/01/2025").}

\item{data_fim}{End date for filtering polls (default: current system date).}

\item{cargo}{Office/position being analyzed (default: "Presidente").}

\item{ambito}{Geographical scope (default: "Brasil").}

\item{cenario}{Electoral scenario (default: "").}

\item{candidaturas_1t}{Character vector of candidates in the 1st round.}

\item{candidaturas_2t}{Character vector of candidates in the 2nd round.}

\item{direita_eleicao_atual}{Character vector of right-wing candidates in the current race. The model can compensate institute errors against right-wing candidates in the last election.}

\item{direita_eleicao_passada}{Name of the right-wing candidate in the previous election.}

\item{esquerda_eleicao_atual}{Character vector of left-wing candidates in the current race. The model can compensate institute errors against left-wing candidates in the last election.}

\item{esquerda_eleicao_passada}{Name of the left-wing candidate in the previous election.}

\item{eleicao_passada_primeiro_turno}{Date of the previous 1st round (e.g., "2/10/2022").}

\item{eleicao_passada_segundo_turno}{Date of the previous 2nd round (e.g., "30/10/2022").}

\item{stan_cores}{Number of CPU cores for Stan to use.}

\item{stan_chains}{Number of MCMC chains.}

\item{stan_warmup}{Number of warmup iterations per chain.}

\item{stan_sampling}{Number of sampling iterations per chain.}

\item{stan_init}{Initial value for Stan parameters.}

\item{stan_adapt_delta}{The target acceptance rate for Stan's NUTS algorithm.}

\item{saida_bases_tratadas}{Directory where treated data will be saved.}

\item{saida_modelos_brutos}{Directory where raw model objects will be saved.}
}
\value{
A list of configuration parameters.
}
\description{
Defines configuration parameters for the poll aggregator, including Stan settings, filters, and election details.
}
\examples{
# Create custom filters and Stan settings
cfg_custom <- configurar_agregador(
  data_inicio = "2025-06-01",
  stan_warmup = 100,
  stan_sampling = 100
)
}
