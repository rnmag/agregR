% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/grafico_priori_posteriori.R
\encoding{UTF-8}
\name{grafico_priori_posteriori}
\alias{grafico_priori_posteriori}
\title{Plot Prior vs Posterior}
\usage{
grafico_priori_posteriori(
  bd,
  candidaturas,
  tipo = "Viés",
  salvar = FALSE,
  config_agregador = configurar_agregador(),
  config_grafico = configurar_grafico(),
  config_prioris = configurar_prioris(bd$nome_modelo),
  dir_saida = "."
)
}
\arguments{
\item{bd}{The results object returned by \code{\link[=rodar_agregador]{rodar_agregador()}}.}

\item{candidaturas}{A character vector of candidate names to include in the plot.}

\item{tipo}{The type of da to plot: "Viés" (for institute bias) or "Percentual" (for candidate voting share).}

\item{salvar}{Logical. If TRUE, saves the plot to disk.}

\item{config_agregador}{A list of configuration parameters created by \code{\link[=configurar_agregador]{configurar_agregador()}}.}

\item{config_grafico}{A list of graphic parameters created by \code{\link[=configurar_grafico]{configurar_grafico()}}.}

\item{config_prioris}{A list of model hyperparameters created by \code{\link[=configurar_prioris]{configurar_prioris()}}.}

\item{dir_saida}{Output directory for the saved plot if \code{salvar = TRUE}.}
}
\value{
A ggplot2 object.
}
\description{
Generates a plot comparing prior and posterior distributions for candidates or bias.
}
\examples{
if (instantiate::stan_cmdstan_exists()) {
  result <- rodar_agregador(
    data_inicio = "01/01/2025",
    turno = 2,
    cenario = "Lula vs Bolsonaro"
  )

  # Prior vs Posterior plot for institute bias
  std_plot <- grafico_priori_posteriori(
    result,
    tipo = "Viés",
    candidaturas = c("Lula", "Bolsonaro")
  )

  # Altering candidate colors
  custom_plot <- grafico_priori_posteriori(
    result,
    candidaturas = c("Lula", "Bolsonaro"),
    config_grafico = configurar_grafico(
      cores_candidaturas = c(Lula = "yelow")
    )
  )
}
}
