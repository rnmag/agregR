---
title: "🇧🇷 Eleições 2026"
---

```{r, include = FALSE}
knitr::opts_chunk$set(
  collapse = TRUE,
  comment = "#>"
)
```

```{r setup}
library(agregR)
```

This article demonstrates how to run models automatically. 
Whenever you push to the repository, GitHub Actions will compile this document and build the `pkgdown` site, evaluating all R chunks along the way.

## Running the Models

You can add your code to run the models here:

```{r run-models, message=FALSE, warning=FALSE}
# Replace this with your actual model running code
# model_fit <- run_model(...)
# summary(model_fit)
```

## Results

```{r results}
# Plot or summarize results here
```
